import {FakeMerchant, FakeReferrer} from "@/utils/constants";

const Consumer: React.FC = () => {
    return (
        <div>
            <h1>Consumer</h1>
        </div>
    );
};

export default Consumer;
